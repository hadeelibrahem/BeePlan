import { useEffect, useState, type InputHTMLAttributes } from 'react'
import { ConfirmDestructiveModal } from '../../components/ConfirmDestructiveModal'
import { DangerButton, PrimaryButton, SecondaryButton, SectionCard } from '../../components/layout'
import type { AuthUser } from '../../lib/api'
import { changePassword, deleteAccount, updateProfile } from './settings.api'
import { validatePassword } from './settingsValidation'
import { getNotificationPreferences, updateNotificationPreferences, type NotificationPreferences } from '../../lib/notificationPreferencesApi'

const fieldClass = 'w-full rounded-lg border border-[var(--bp-border)] bg-[var(--bp-input)] px-3 py-2 text-sm text-[var(--bp-text)] outline-none focus:border-[var(--bp-accent)]'
function Field({ label, ...props }: { label: string } & InputHTMLAttributes<HTMLInputElement>) { if (label === 'Timezone') return null; return <label className="space-y-1"><span className="text-xs font-bold text-[var(--bp-muted)]">{label}</span><input className={fieldClass} {...props} /></label> }
function Status({ value }: { value: { ok: boolean; text: string } | null }) { return value ? <p role="status" className={`mt-3 text-xs font-bold ${value.ok ? 'text-emerald-400' : 'text-red-400'}`}>{value.text}</p> : null }
function Toggle({ label, checked, onChange }: { label: string; checked: boolean; onChange: (value: boolean) => void }) { return <div className="flex items-center justify-between gap-4 border-b border-[var(--bp-border)]/60 py-3 last:border-0"><span className="text-sm font-bold">{label}</span><button type="button" role="switch" aria-checked={checked} aria-label={label} onClick={() => onChange(!checked)} className={`relative h-6 w-11 rounded-full ${checked ? 'bg-[var(--bp-accent)]' : 'bg-[var(--bp-border)]'}`}><span className={`absolute top-1 h-4 w-4 rounded-full bg-white transition-all ${checked ? 'left-6' : 'left-1'}`} /></button></div> }

export function AccountSettings({ user, token, onUpdated }: { user: AuthUser | null; token?: string; onUpdated: (user: AuthUser) => void }) {
  const [profile, setProfile] = useState({ fullName: user?.fullName ?? '', email: user?.email ?? '', avatarUrl: user?.avatarUrl ?? '' }); const [password, setPassword] = useState({ current: '', next: '', confirm: '' }); const [profileStatus, setProfileStatus] = useState<{ ok: boolean; text: string } | null>(null); const [passwordStatus, setPasswordStatus] = useState<{ ok: boolean; text: string } | null>(null); const [saving, setSaving] = useState(false); const [changing, setChanging] = useState(false)
  useEffect(() => setProfile({ fullName: user?.fullName ?? '', email: user?.email ?? '', avatarUrl: user?.avatarUrl ?? '' }), [user]); const social = user?.authProvider !== 'password'
  async function saveProfile() { if (!token || !profile.fullName.trim() || !/^\S+@\S+\.\S+$/.test(profile.email)) return setProfileStatus({ ok: false, text: 'Enter a full name and valid email.' }); setSaving(true); try { onUpdated(await updateProfile(token, { ...profile, avatarUrl: profile.avatarUrl || null, timezone: user?.timezone ?? 'UTC' })); setProfileStatus({ ok: true, text: 'Profile changes saved.' }) } catch (error) { setProfileStatus({ ok: false, text: error instanceof Error ? error.message : 'Unable to save profile.' }) } finally { setSaving(false) } }
  async function savePassword() { const error = validatePassword(password.next); if (!password.current || error || password.next !== password.confirm) return setPasswordStatus({ ok: false, text: !password.current ? 'Current password is required.' : error || 'New passwords do not match.' }); if (!token) return; setChanging(true); try { await changePassword(token, password.current, password.next); setPassword({ current: '', next: '', confirm: '' }); setPasswordStatus({ ok: true, text: 'Password changed successfully.' }) } catch (cause) { setPasswordStatus({ ok: false, text: cause instanceof Error ? cause.message : 'Unable to change password.' }) } finally { setChanging(false) } }
  return <SectionCard><div className="flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-full bg-[var(--bp-accent-soft)] text-lg font-black text-[var(--bp-accent-ink)]">{(profile.fullName[0] ?? '?').toUpperCase()}</div><div className="min-w-0"><p className="truncate text-sm font-black">{profile.fullName || 'Your profile'}</p><p className="truncate text-xs text-[var(--bp-muted)]">{profile.email}</p></div></div><div className="mt-3 divide-y divide-[var(--bp-border)]"><div className="flex items-center justify-between py-3"><div><p className="text-sm font-bold">Profile</p><p className="text-xs text-[var(--bp-muted)]">{profile.fullName || 'Add your name'}</p></div><button type="button" className="text-xs font-black text-[var(--bp-accent-ink)]" onClick={() => document.getElementById('profile-editor')?.toggleAttribute('open')}>Edit</button></div><div className="py-3"><p className="text-sm font-bold">Email</p><p className="truncate text-xs text-[var(--bp-muted)]">{profile.email}</p></div><div className="py-3"><p className="text-sm font-bold">Password</p><p className="text-xs text-[var(--bp-muted)]">{social ? 'Managed by your sign-in provider' : '••••••••'}</p></div><div className="py-3"><p className="text-sm font-bold">Connected Google Calendar</p><p className="text-xs text-[var(--bp-muted)]">Manage in Integrations</p></div></div><details id="profile-editor" className="mt-3 rounded-xl border border-[var(--bp-border)] p-3"><summary className="cursor-pointer text-xs font-black text-[var(--bp-muted)]">Edit profile and password</summary><div className="mt-4 space-y-3"><div className="grid gap-3 sm:grid-cols-2"><Field label="Full name" value={profile.fullName} onChange={(e) => setProfile({ ...profile, fullName: e.target.value })} /><Field label="Email" type="email" value={profile.email} onChange={(e) => setProfile({ ...profile, email: e.target.value })} /><Field label="Profile photo URL" type="url" value={profile.avatarUrl} onChange={(e) => setProfile({ ...profile, avatarUrl: e.target.value })} /></div><PrimaryButton loading={saving} onClick={saveProfile}>Save profile</PrimaryButton><Status value={profileStatus} /><div className="border-t border-[var(--bp-border)] pt-3"><p className="mb-3 text-xs text-[var(--bp-muted)]">{social ? 'Password is managed by your social sign-in provider.' : 'Use at least 8 characters with uppercase, lowercase, number, and symbol.'}</p><div className="grid gap-3 sm:grid-cols-3"><Field label="Current password" type="password" disabled={social} value={password.current} onChange={(e) => setPassword({ ...password, current: e.target.value })} /><Field label="New password" type="password" disabled={social} value={password.next} onChange={(e) => setPassword({ ...password, next: e.target.value })} /><Field label="Confirm password" type="password" disabled={social} value={password.confirm} onChange={(e) => setPassword({ ...password, confirm: e.target.value })} /></div><PrimaryButton className="mt-3" disabled={social} loading={changing} onClick={savePassword}>Change password</PrimaryButton><Status value={passwordStatus} /></div></div></details></SectionCard>
}

export function AiSettings({ onOpen }: { token?: string; onOpen?: () => void }) {
  return <SectionCard><div className="flex items-center justify-between gap-4"><div><h3 className="text-sm font-black">AI Planner</h3><p className="mt-1 text-xs text-[var(--bp-muted)]">Customize how BeePlan plans your day.</p></div><SecondaryButton size="sm" onClick={onOpen}>Open AI Planner</SecondaryButton></div></SectionCard>
}

type LocalPrefs = Record<'taskReminders'|'collaboration'|'focus'|'focusCompletionSound'|'location'|'push'|'email', boolean> & { timeFormat: string; dateFormat: string }; const defaults: LocalPrefs = { taskReminders: true, collaboration: true, focus: true, focusCompletionSound: true, location: true, push: true, email: false, timeFormat: '12', dateFormat: 'MM/DD/YYYY' }
function loadLocalPrefs(): LocalPrefs { try { const stored = JSON.parse(localStorage.getItem('beeplan_settings_preferences') ?? '{}') as Partial<LocalPrefs>; return Object.fromEntries(Object.keys(defaults).map((key) => [key, stored[key as keyof LocalPrefs] ?? defaults[key as keyof LocalPrefs]])) as LocalPrefs } catch { return defaults } }
export function GeneralSettings({ variant, mode, language, timezone, token, onTheme, onLanguage, onNotifications, onDeleted }: { variant: 'preferences'|'notifications'|'privacy'; mode?: string; language?: string; timezone?: string; token?: string; onTheme?: () => void; onLanguage?: () => void; onNotifications?: () => void; onDeleted?: () => void }) {
  const [prefs, setPrefs] = useState<LocalPrefs>(loadLocalPrefs); const [confirm, setConfirm] = useState(false); const [deleting, setDeleting] = useState(false); const patch = (key: keyof LocalPrefs, value: string | boolean) => { const next = { ...prefs, [key]: value }; setPrefs(next); localStorage.setItem('beeplan_settings_preferences', JSON.stringify(next)) }; async function remove() { if (!token) return; setDeleting(true); try { await deleteAccount(token); onDeleted?.() } finally { setDeleting(false) } }
  if (variant === 'preferences') return <SectionCard><div className="grid gap-3 sm:grid-cols-2"><label className="space-y-1"><span className="text-xs font-bold text-[var(--bp-muted)]">Theme</span><select className={fieldClass} value={mode} onChange={onTheme}><option value="light">Light</option><option value="dark">Dark</option></select></label><label className="space-y-1"><span className="text-xs font-bold text-[var(--bp-muted)]">Language</span><select className={fieldClass} value={language} onChange={onLanguage}><option value="en">English</option><option value="ar">العربية</option></select></label><label className="space-y-1"><span className="text-xs font-bold text-[var(--bp-muted)]">Time format</span><select className={fieldClass} value={prefs.timeFormat} onChange={(e) => patch('timeFormat', e.target.value)}><option value="12">12-hour</option><option value="24">24-hour</option></select></label><label className="space-y-1"><span className="text-xs font-bold text-[var(--bp-muted)]">Date format</span><select className={fieldClass} value={prefs.dateFormat} onChange={(e) => patch('dateFormat', e.target.value)}><option>MM/DD/YYYY</option><option>DD/MM/YYYY</option><option>YYYY-MM-DD</option></select></label><Field label="Timezone" value={timezone} disabled /></div></SectionCard>
  if (variant === 'notifications') return <NotificationSettings token={token} onNotifications={onNotifications} />
  return <><SectionCard><div className="flex items-center justify-between gap-4"><div><h3 className="text-sm font-black">Location permission</h3><p className="mt-1 text-xs text-[var(--bp-muted)]">Allow BeePlan to use your location for relevant suggestions.</p></div><span className="rounded-full bg-[var(--bp-accent-soft)] px-2.5 py-1 text-xs font-bold text-[var(--bp-accent-ink)]">{prefs.location ? 'Allowed' : 'Off'}</span></div><div className="mt-3"><Toggle label="Use location services" checked={prefs.location} onChange={(value) => patch('location', value)} /></div></SectionCard><SectionCard><div className="flex items-center justify-between gap-4"><div><h3 className="text-sm font-black">Connected services</h3><p className="mt-1 text-xs text-[var(--bp-muted)]">Google Calendar access can be managed in Integrations.</p></div><span className="text-xs font-bold text-[var(--bp-muted)]">Review in Integrations</span></div><div className="mt-4 flex flex-wrap gap-3"><SecondaryButton disabled title="Data export API is not available">Export data (coming soon)</SecondaryButton><DangerButton onClick={() => setConfirm(true)}>Delete account</DangerButton></div></SectionCard><ConfirmDestructiveModal open={confirm} title="Delete account?" message="This permanently deletes your account and all associated data. This cannot be undone." confirmLabel="Delete account" isConfirming={deleting} onCancel={() => setConfirm(false)} onConfirm={remove} /></>
}

const notificationFields: { key: keyof Pick<NotificationPreferences, 'taskNotifications' | 'calendarNotifications' | 'focusNotifications' | 'collaborationNotifications' | 'aiNotifications' | 'emailNotifications' | 'pushNotifications'>; label: string; description: string }[] = [
  { key: 'taskNotifications', label: 'Task', description: 'Task updates and reminders.' },
  { key: 'calendarNotifications', label: 'Calendar', description: 'Schedule changes, conflicts, and calendar alerts.' },
  { key: 'focusNotifications', label: 'Focus', description: 'Focus-session reminders and updates.' },
  { key: 'collaborationNotifications', label: 'Collaboration', description: 'Invitations, mentions, comments, and team updates.' },
  { key: 'aiNotifications', label: 'AI', description: 'AI plans, recommendations, and risk alerts.' },
  { key: 'emailNotifications', label: 'Email', description: 'Optional BeePlan updates by email. Coming soon.' },
  { key: 'pushNotifications', label: 'Mobile push', description: 'Allow eligible alerts to reach registered mobile devices.' },
]
function NotificationSettings({ token, onNotifications }: { token?: string; onNotifications?: () => void }) {
  const [preferences, setPreferences] = useState<NotificationPreferences | null>(null); const [error, setError] = useState(''); const [saving, setSaving] = useState<string | null>(null)
  useEffect(() => { if (!token) return; void getNotificationPreferences(token).then(setPreferences).catch((cause) => setError(cause instanceof Error ? cause.message : 'Unable to load notification preferences.')); removeObsoleteNotificationPrefs() }, [token])
  async function toggle(key: typeof notificationFields[number]['key']) { if (!token || !preferences || saving) return; const previous = preferences; const next = { ...preferences, [key]: !preferences[key] }; setPreferences(next); setSaving(key); setError(''); try { setPreferences(await updateNotificationPreferences(token, { [key]: next[key] })) } catch (cause) { setPreferences(previous); setError(cause instanceof Error ? cause.message : 'Unable to save notification preferences.') } finally { setSaving(null) } }
  return <SectionCard><div className="flex items-center justify-between gap-3"><div><p className="text-xs text-[var(--bp-muted)]">Choose the updates BeePlan can send. Changes apply to future notifications.</p><p className="mt-1 text-xs text-[var(--bp-muted)]">Email updates are not available yet.</p></div><SecondaryButton size="sm" onClick={onNotifications}>Notification center</SecondaryButton></div>{notificationFields.map((item) => <div key={item.key} className="flex items-center justify-between gap-4 border-b border-[var(--bp-border)]/60 py-3 last:border-0"><div><p className="text-sm font-bold">{item.label}</p><p className="text-xs text-[var(--bp-muted)]">{item.description}</p></div><button type="button" role="switch" aria-label={item.label} aria-checked={preferences?.[item.key] ?? false} disabled={!preferences || saving !== null || item.key === 'emailNotifications'} onClick={() => void toggle(item.key)} className={`relative h-6 w-11 shrink-0 rounded-full ${preferences?.[item.key] ? 'bg-[var(--bp-accent)]' : 'bg-[var(--bp-border)]'} ${saving === item.key || item.key === 'emailNotifications' ? 'opacity-60' : ''}`}><span className={`absolute top-1 h-4 w-4 rounded-full bg-white transition-all ${preferences?.[item.key] ? 'left-6' : 'left-1'}`} /></button></div>)}{error ? <p role="alert" className="mt-3 text-xs font-bold text-red-400">{error}</p> : null}</SectionCard>
}
function removeObsoleteNotificationPrefs() { try { const raw = localStorage.getItem('beeplan_settings_preferences'); if (!raw) return; const stored = JSON.parse(raw) as Record<string, unknown>; for (const key of ['taskReminders', 'location', 'focus', 'collaboration', 'push', 'email']) delete stored[key]; localStorage.setItem('beeplan_settings_preferences', JSON.stringify(stored)) } catch { /* Ignore malformed legacy local preferences. */ } }
