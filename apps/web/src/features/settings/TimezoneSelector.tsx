import { useMemo, useState } from 'react';
import type { AuthUser } from '../../lib/api';
import { updateProfile } from './settings.api';

const COMMON: Array<[string, string]> = [['Asia/Hebron', 'Palestine'], ['Asia/Amman', 'Amman'], ['Europe/London', 'London'], ['America/New_York', 'New York'], ['America/Los_Angeles', 'Los Angeles'], ['UTC', 'UTC']];
function allZones() { const supported = typeof Intl.supportedValuesOf === 'function' ? Intl.supportedValuesOf('timeZone') : []; const map = new Map(COMMON); for (const id of supported) if (!map.has(id)) map.set(id, id.split('/').at(-1)?.replaceAll('_', ' ') ?? id); return [...map].map(([id, city]) => ({ id, city })); }
function display(id: string, city: string) { const offset = new Intl.DateTimeFormat('en', { timeZone: id, timeZoneName: 'longOffset' }).formatToParts(new Date()).find((part) => part.type === 'timeZoneName')?.value?.replace('GMT', 'UTC') ?? 'UTC'; return `${city} (${id}) — ${offset}`; }
export function TimezoneSelector({ user, token, onUpdated }: { user: AuthUser; token: string; onUpdated: (user: AuthUser) => void }) { const zones = useMemo(allZones, []); const [value, setValue] = useState(user.timezone); const [query, setQuery] = useState(''); const [status, setStatus] = useState<string | null>(null); const filtered = zones.filter((item) => `${item.city} ${item.id}`.toLowerCase().includes(query.toLowerCase())).slice(0, 80); const save = async (next: string) => { if (next === user.timezone) return; setValue(next); setStatus('Saving…'); try { const updated = await updateProfile(token, { fullName: user.fullName, email: user.email, avatarUrl: user.avatarUrl, timezone: next }); onUpdated(updated); setStatus('Saved'); } catch (error) { setValue(user.timezone); setStatus(error instanceof Error ? error.message : 'Unable to save timezone.'); } }; const detected = Intl.DateTimeFormat().resolvedOptions().timeZone || 'UTC'; return <div className="space-y-2"><label className="space-y-1"><span className="text-xs font-bold text-[var(--bp-muted)]">Timezone</span><input className="w-full rounded-lg border border-[var(--bp-border)] bg-[var(--bp-input)] px-3 py-2 text-sm text-[var(--bp-text)]" placeholder="Search city or timezone" value={query} onChange={(event) => setQuery(event.target.value)} /><select aria-label="Timezone" className="w-full rounded-lg border border-[var(--bp-border)] bg-[var(--bp-input)] px-3 py-2 text-sm text-[var(--bp-text)]" value={value} onChange={(event) => void save(event.target.value)}>{filtered.map((item) => <option key={item.id} value={item.id}>{display(item.id, item.city)}</option>)}</select></label>{user.timezone === 'UTC' && detected !== 'UTC' ? <button type="button" className="text-xs font-bold text-[var(--bp-accent-ink)]" onClick={() => void save(detected)}>Use device timezone ({detected})</button> : null}{status ? <p role="status" className="text-xs font-bold text-[var(--bp-muted)]">{status}</p> : null}</div>; }
