import { useMemo, useState } from 'react'
import { Image, Pressable, ScrollView, Text, TextInput, View } from 'react-native'
import { AppScreen } from '../components/layout'
import { useTheme } from '../theme/useTheme'

type Achievement = { id: string; title: string; category: string; achievementDate: string; description: string; reflection: string; images: { id: string; url: string; isCover: boolean; fileName: string }[]; stats: { completedTasks: number; focusSessions: number; focusedMinutes: number } | null }
type Props = { items: Achievement[]; token: string; onOpen: (item: Achievement) => void; onAdd: () => void; onReview: (year: number) => void; ImageComponent: React.ComponentType<{ token: string; image: Achievement['images'][number]; className?: string }> }
const coverOf = (item: Achievement) => item.images.find(image => image.isCover) ?? item.images[0]

export default function MobileMuseumHome({ items, token, onOpen, onAdd, onReview, ImageComponent }: Props) {
  const { colors } = useTheme().theme; const [query, setQuery] = useState(''); const [category, setCategory] = useState('All'); const [year, setYear] = useState('All'); const [filters, setFilters] = useState(false)
  const years = useMemo(() => [...new Set(items.map(item => item.achievementDate.slice(0, 4)))].sort().reverse(), [items]); const categories = useMemo(() => [...new Set(items.map(item => item.category))].sort(), [items]); const visible = items.filter(item => (!query || `${item.title} ${item.description}`.toLowerCase().includes(query.toLowerCase())) && (category === 'All' || item.category === category) && (year === 'All' || item.achievementDate.startsWith(year))); const featured = items.find(item => item.images.length && (item.description || item.reflection)) ?? items[0]; const halls = visible.reduce<Record<string, Achievement[]>>((out, item) => { (out[item.achievementDate.slice(0, 4)] ??= []).push(item); return out }, {})
  return <AppScreen><ScrollView contentContainerStyle={{ paddingBottom: 44 }}><View className="px-5 pt-6"><Text className="text-xs font-black uppercase tracking-[3px]" style={{ color: colors.accent }}>Achievement Museum</Text><Text className="mt-3 text-5xl font-black" style={{ color: colors.text }}>Your <Text style={{ color: colors.accent }}>Journey.</Text></Text><Text className="mt-3 text-base leading-6" style={{ color: colors.secondaryText }}>A collection of the moments, projects, and milestones that shaped your path.</Text><Text className="mt-3 text-sm font-bold" style={{ color: colors.secondaryText }}>{items.length} {items.length === 1 ? 'achievement' : 'achievements'} · Since {items.length ? Math.min(...items.map(item => Number(item.achievementDate.slice(0, 4)))) : new Date().getFullYear()}</Text>{items.length > 0 && coverOf(items[0]) ? <View className="mt-6 flex-row gap-2"><View className="flex-1"><ImageComponent token={token} image={coverOf(items[0])} className="h-48 w-full rounded-2xl"/></View><View className="w-[32%] gap-2">{items.slice(1, 3).map(item => coverOf(item) ? <ImageComponent key={item.id} token={token} image={coverOf(item)} className="h-[92px] w-full rounded-2xl"/> : null)}</View></View> : null}</View><View className="mt-7 border-y px-5 py-3" style={{ borderColor: colors.border }}><View className="flex-row items-center justify-between"><Text className="text-xs font-black uppercase tracking-wider" style={{ color: colors.secondaryText }}>Collection</Text><Pressable accessibilityRole="button" accessibilityLabel="Open museum filters" onPress={() => setFilters(!filters)} className="rounded-lg border px-3 py-2" style={{ borderColor: colors.border }}><Text className="text-xs font-bold" style={{ color: colors.text }}>{filters ? 'Done' : 'Filter'}</Text></Pressable></View>{filters ? <View className="mt-3 gap-2"><TextInput value={query} onChangeText={setQuery} placeholder="Search your museum" placeholderTextColor={colors.placeholder} className="rounded-xl border px-3 py-3" style={{ borderColor: colors.border, backgroundColor: colors.input, color: colors.text }}/><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={{ gap: 8 }}>{['All', ...years, ...categories].map(value => <Pressable key={value} onPress={() => years.includes(value) || value === 'All' ? setYear(value) : setCategory(value)} className="rounded-full border px-3 py-2" style={{ borderColor: (year === value || category === value) ? colors.accent : colors.border }}><Text className="text-xs font-bold" style={{ color: colors.text }}>{value}</Text></Pressable>)}</ScrollView></View> : null}</View>{featured && !query && category === 'All' && year === 'All' && coverOf(featured) ? <View className="mt-7 px-5"><Text className="text-xs font-black uppercase tracking-widest" style={{ color: colors.accent }}>Featured Exhibit</Text><Pressable onPress={() => onOpen(featured)} className="mt-3 overflow-hidden rounded-2xl border" style={{ borderColor: colors.border, backgroundColor: colors.surface }}><ImageComponent token={token} image={coverOf(featured)} className="h-56 w-full"/><View className="p-4"><Text className="text-xs font-black uppercase" style={{ color: colors.secondaryText }}>{featured.category} · {featured.achievementDate}</Text><Text className="mt-2 text-2xl font-black" style={{ color: colors.text }}>{featured.title}</Text><Text numberOfLines={2} className="mt-2 leading-5" style={{ color: colors.secondaryText }}>{featured.description || featured.reflection || 'Explore this moment from your journey.'}</Text><Text className="mt-3 text-xs font-black" style={{ color: colors.accentInk }}>Explore exhibit →</Text></View></Pressable></View> : null}<View className="mt-8 px-5">{Object.entries(halls).map(([hallYear, hall], index) => <View key={hallYear} className="mb-8"><View className="flex-row items-end justify-between border-y py-4" style={{ borderColor: colors.border }}><View><Text className="text-xs font-black uppercase tracking-widest" style={{ color: colors.accent }}>Hall {String(index + 1).padStart(2, '0')}</Text><Text className="mt-1 text-4xl font-black" style={{ color: colors.text }}>{hallYear}</Text></View><View className="items-end"><Text className="text-sm" style={{ color: colors.secondaryText }}>{hall.length} moments</Text><Pressable onPress={() => onReview(Number(hallYear))}><Text className="mt-1 text-xs font-black" style={{ color: colors.accentInk }}>Year in Review →</Text></Pressable></View></View>{hall.map(item => <Pressable key={item.id} onPress={() => onOpen(item)} className="mt-5 overflow-hidden rounded-2xl border" style={{ borderColor: colors.border, backgroundColor: colors.surface }}>{coverOf(item) ? <ImageComponent token={token} image={coverOf(item)} className="h-52 w-full"/> : <View className="h-32" style={{ backgroundColor: colors.accentSoft }}/> }<View className="p-4"><Text className="text-xs font-black uppercase" style={{ color: colors.accent }}>{item.category} · {item.achievementDate}</Text><Text className="mt-2 text-xl font-black" style={{ color: colors.text }}>{item.title}</Text>{item.description || item.reflection ? <Text numberOfLines={2} className="mt-2 leading-5" style={{ color: colors.secondaryText }}>“{item.description || item.reflection}”</Text> : null}<Text className="mt-3 text-xs font-black" style={{ color: colors.accentInk }}>Explore exhibit →</Text></View></Pressable>)}</View>)}{!visible.length ? <View className="items-center py-16"><Text className="text-lg font-black" style={{ color: colors.text }}>{items.length ? 'No exhibits match those filters.' : 'Your museum is waiting.'}</Text><Text className="mt-2 text-center" style={{ color: colors.secondaryText }}>{items.length ? 'Try clearing your filters.' : 'Save the moments you want to remember.'}</Text></View> : null}</View><Pressable accessibilityRole="button" onPress={onAdd} className="mx-5 mt-2 rounded-xl p-4" style={{ backgroundColor: colors.accent }}><Text className="text-center font-black" style={{ color: colors.accentText }}>+ Add Achievement</Text></Pressable></ScrollView></AppScreen>
}
