# For local backend testing, use your computer's LAN IP (e.g. http://192.168.1.x:3000) —
# "localhost"/"127.0.0.1" only resolve to the phone/emulator itself, not your dev machine.
# The phone and the backend must be on the same Wi-Fi network.
EXPO_PUBLIC_API_URL=https://harmonious-transformation-production-bd55.up.railway.app
EXPO_PUBLIC_GEOAPIFY_API_KEY=
